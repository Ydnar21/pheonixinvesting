# Compiled files (Always ignore these)
node_modules/
npm-debug.log
yarn-error.log

# Source files (We need these for the build step, so they are NOT ignored)
# The lines for 'src/' and 'public/' that were here before have been REMOVED.

# Development files (Ignore sensitive/local files)
.env.local
.env.development
*.local
.env

# Git
.git
.gitignore

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Config files (Do NOT ignore these; the build needs them)
.eslintrc.*
.prettierrc
tsconfig*.json
vite.config.ts
tailwind.config.js
postcss.config.js

# Misc
.DS_Store
*.log
coverage/
.nyc_output/

# Build tools
*.md
!dist/**
