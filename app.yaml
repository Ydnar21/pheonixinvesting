runtime: nodejs20

instance_class: F1

env_variables:
  NODE_ENV: 'production'
  # ----------------------------------------------------
  # FIX: VITE Build-Time Variables must be passed here
  # These values will be available during the 'npm run build' step on Cloud Build
  # ----------------------------------------------------
  VITE_SUPABASE_URL: https://hcrzudqxnytnizltnqnz.supabase.co 
  VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imhjcnp1ZHF4bnl0bml6bHRucW56Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE3OTA4NzEsImV4cCI6MjA3NzM2Njg3MX0.fUByWCvc6gQcDQECZxmuSZNq1lzmqHc_yR36-MLwT6w 

handlers:
  # 1. Explicitly serve everything in the 'dist/assets' folder (JS, CSS bundles)
  - url: /assets
    static_dir: dist/assets
    secure: always
    expiration: "7d"
    http_headers:
      Cache-Control: "public, max-age=604800, immutable"

  # 2. Serve specific root-level static files (images, fonts, etc.).
  - url: /(.*\.(png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot))$
    static_files: dist/\1
    upload: dist/(.*\.(png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot))$
    secure: always
    expiration: "7d"
    http_headers:
      Cache-Control: "public, max-age=604800, immutable"
      
  # 3. SPA Fallback: All remaining routes (/, /portfolio, /community) go to index.html
  - url: /.*
    static_files: dist/index.html
    upload: dist/index.html
    secure: always
    http_headers:
      Cache-Control: "no-cache, no-store, must-revalidate"

automatic_scaling:
  min_idle_instances: 0
  max_idle_instances: 1
  min_pending_latency: automatic
  max_pending_latency: automatic
